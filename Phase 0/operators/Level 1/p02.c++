#include <bits/stdc++.h>
using namespace std;

int main(){

    int num;
    cout << "Enter the number: ";
    cin >> num;

    if(num > 10){
        cout << num << " is Greater than 10" <<endl;
    }
    else {
        cout << num << " is smaller than 10" <<endl;
    }

    return 0;
}